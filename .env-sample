PROJECT_ENDPOINT=<Azure AI Foundry プロジェクトのエンドポイント>
BING_RESOURCE_NAME=<Bing Search のリソース名>
DEEP_RESEARCH_MODEL_DEPLOYMENT_NAME=<o3-deep-researchのデプロイ名>
MODEL_DEPLOYMENT_NAME=<モデルのデプロイ名>

# deep-research.py用の接続ID
# 接続IDを手動で追加する場合（Azure AI Foundryポータルで確認）
# 正しい形式: /subscriptions/<subscription_id>/resourceGroups/<resource_group_name>/providers/<provider_name>/accounts/<account_name>/projects/<project_name>/connections/<connection_name>
# BING_CONNECTION_ID=your-full-connection-id-here
BING_CONNECTION_ID=<Bing Search のリソースID>

# deep-research-async.py用の接続ID（同じ値だが変数名が異なる）
AZURE_BING_CONNECTION_ID=<Bing Search のリソースID>

